{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run start --scope docs --stream",
    "deploy": "lerna run deploy --scope docs --stream",
    "install": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
